import random
import string

# Ask the user how many ec2 instance names to generate
num_instances = int(input('How many ec2 instance names do you want to generate?\n'))

# Ask the user how long each instance name should be
name_length = int(input("How long should each ec2 instance name be?\n"))

# Ask the user the name of their department
department = input('What is the name of your department?\n')

# Generate random letter and number string
chars = string.ascii_uppercase + string.ascii_lowercase + string.digits
random_name = ''.join(random.choice(chars) for i in range(name_length))

print("Here are your ec2 instance names:\n")

# Generate the ec2 instance names
for i in range(num_instances):
    name = ""
    for j in range(name_length):
        name += random.choice(chars)
    print(name)
